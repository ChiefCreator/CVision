import React from "react";

import type { CoverLetterTemplateRendererProps } from "./TemplateRenderer";

export default React.memo(function CoverLetterTemplateRenderer({}: CoverLetterTemplateRendererProps) {
  return "CoverLetter"
});
